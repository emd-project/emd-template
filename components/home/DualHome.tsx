/**
 * DEPRECATED — la variante home « Dual » (portail) a été retirée du système de
 * variantes (remplacée par « marché » et « fil »). Ce fichier n'est référencé
 * nulle part (ni HomeRouter, ni resolver, ni preview) et peut être supprimé.
 * Conservé temporairement car l'API d'édition ne permet pas la suppression.
 */
export {}
