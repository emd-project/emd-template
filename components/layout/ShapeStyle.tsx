/**
 * DEPRECATED — renommé en PermutationStyle (shape + border + shadow).
 * Ce ré-export évite toute casse d'import existant.
 */
export { PermutationStyle as ShapeStyle } from '@/components/layout/PermutationStyle'
