/* Supprimé — modèle MENTION : aucune affiliation. Le module testé n'existe plus.
   Vestige vide (un test neutre garde `vitest run` vert) — à supprimer du repo. */
import { describe, it, expect } from 'vitest'

describe('vestige', () => {
  it('fichier à supprimer', () => {
    expect(true).toBe(true)
  })
})
