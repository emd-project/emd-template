# ⚠️ DOSSIER DÉPRÉCIÉ

La doctrine vit dans **`emd-project/emd-methodo`** (`skills/` + `references/`).
Ce dossier sera **supprimé** ; ne **PAS** le lire, ne **PAS** le mettre à jour.
Les tâches de rédaction lisent methodo via `github_read_file` (cf. `docs/SCHEDULED-TASK-REDACTION.md`).
Seuls les fichiers propres au site (`content/ton-of-voice.md`, `docs/AUTHOR-*.md`, `niche.config.ts`) restent locaux.
